---
extends: default
ignore:
  - '*.md'
  - '*.env'
  - '*.txt'
  - '*.sh'
  # Helm templates are not valid YAML until rendered (Go templating).
  - 'charts/**/templates/**'

rules:
  line-length:
    max: 256
    level: warning
